% Methodology

\section{Problem Formulation}

This research addresses the fundamental question: what communication range optimizes harvesting efficiency in multi-agent systems? This section formalizes the problem by defining the state space, action space, objective function, and constraints.

\subsection{Problem Definition}

Given a team of $N$ autonomous harvesting agents operating in a spatial environment with distributed fruit resources, the goal is to maximize collective harvest yield. Agents can communicate within a configurable range $r$ (measured in grid cells using Chebyshev distance). The problem is to identify the communication range that optimizes the trade-off between information sharing benefits and communication costs.

\textbf{State Space}: Each agent maintains local state including position $(x, y)$, known fruit locations, and messages received from neighbors.

\textbf{Action Space}: At each time step, agents can move (8 directions), harvest fruit if present, and broadcast messages to neighbors within range $r$.

\textbf{Objective Function}: Maximize total yield $Y = \sum_{t=1}^{T} \sum_{i=1}^{N} h_i(t)$ where $h_i(t)$ is fruit harvested by agent $i$ at time $t$.

\textbf{Constraints}:
\begin{itemize}
    \item Agents have limited sensing range (can only see fruit in adjacent cells)
    \item Communication range $r$ is fixed for each experimental run
    \item Fruit cannot be harvested by multiple agents simultaneously (only one agent can harvest each fruit)
    \item Multiple agents may occupy the same cell (using Mesa's MultiGrid)
\end{itemize}

\subsection{Research Approach}

Rather than attempting to find a globally optimal communication strategy (which would require solving a complex multi-agent optimization problem), I take an empirical approach: systematically test discrete communication ranges and analyze performance using statistical methods.

\section{Methodological Approach}

Multiple approaches exist for investigating communication range effects in multi-agent systems. Machine learning methods (reinforcement learning, evolutionary algorithms) could discover optimal communication policies, but they require extensive training, hyperparameter tuning, and produce results that are difficult to interpret. Given the constraints of an MSc dissertation (limited time, focus on understanding mechanisms rather than optimization), I chose a systematic parameter sweep approach: test discrete communication ranges $r \in \{0, 2, 4, 6, 8\}$ through controlled experimentation and analyze results using descriptive statistics.

This approach directly answers the research question ("Does communication range affect harvesting efficiency?") with clear, interpretable results. It allows systematic exploration of how communication range interacts with team size and resource density. While it doesn't search continuous parameter space or discover novel strategies, it provides rigorous evidence of communication range effects without the computational overhead or interpretability challenges of machine learning methods. This balance between rigor and feasibility makes it well-suited for an MSc research project focused on RQ1 (communication range effects) in static environments.

\section{Simulation Environment Design}

\subsection{Framework Selection: Mesa 3.0}

This research employs Mesa 3.0, a Python framework for agent-based modeling. Mesa provides essential simulation infrastructure including grid management, agent scheduling, and data collection, enabling focus on domain-specific agent behaviors and coordination mechanisms.

\textbf{Key features}:
\begin{itemize}
    \item \texttt{MultiGrid}: Spatial environment with multiple agents per cell
    \item \texttt{model.agents.shuffle\_do('step')}: Randomized agent activation each step
    \item \texttt{DataCollector}: Automated metric collection over time
    \item \texttt{SolaraViz}: Interactive web-based visualization
\end{itemize}

\subsection{Environment Specification}

\textbf{Grid}: 50×50 cells, non-toroidal (agents cannot wrap around edges). This size was chosen to provide sufficient spatial complexity for communication range effects to manifest while remaining computationally tractable for 400 experimental runs.

\textbf{Fruit placement}: Random initial placement with density $\rho \in \{0.15, 0.25\}$ (proportion of cells with fruit)

\textbf{Resource dynamics}: Static mode only. Fruit doesn't regenerate once harvested. This simplification allows us to isolate the effects of communication range on harvesting efficiency without confounding factors from resource regeneration.

\subsection{Agent Architecture}

Agents follow a simplified BDI (Belief-Desire-Intention) architecture inspired by the Multi-Agent Systems module:

\textbf{Beliefs}: Known fruit locations (from sensing and received messages)

\textbf{Desires}: Harvest fruit to maximize collective yield

\textbf{Intentions}: Move toward nearest known fruit, or explore if none known

\textbf{Agent behavior algorithm}:
\begin{enumerate}
    \item \textbf{Sense}: Detect fruit in adjacent cells (Moore neighborhood)
    \item \textbf{Receive messages}: Update beliefs with fruit locations from neighbors within range $r$
    \item \textbf{Decide}: Choose action based on beliefs
    \begin{itemize}
        \item If fruit in current cell: harvest
        \item Else if known fruit locations: move toward nearest
        \item Else: random walk (exploration)
    \end{itemize}
    \item \textbf{Act}: Execute chosen action
    \item \textbf{Communicate}: Broadcast known fruit locations to neighbors within range $r$
\end{enumerate}

\subsection{Communication Protocol}

\textbf{Range}: Chebyshev distance $d_{Chebyshev}(a, b) = \max(|x_a - x_b|, |y_a - y_b|)$

\textbf{Message content}: List of known fruit locations (coordinates)

\textbf{Broadcasting}: Each agent sends messages to all agents within range $r$ each step

\textbf{Message processing}: Receiving agents merge new locations into their belief set

\textbf{Cost model}: Count total messages sent (bandwidth proxy)

\section{Experimental Design}

\subsection{Independent Variables}

\textbf{Communication range} $r \in \{0, 2, 4, 6, 8\}$ cells (5 levels)
\begin{itemize}
    \item $r=0$: No communication (baseline)
    \item $r=2$: Local communication (immediate neighbors)
    \item $r=4$: Medium range
    \item $r=6$: Long range
    \item $r=8$: Very long range (covers significant portion of 50×50 grid)
\end{itemize}

\textbf{Team size} $N \in \{10, 20\}$ agents (2 levels)

\textbf{Resource density} $\rho \in \{0.15, 0.25\}$ (2 levels)

\textbf{Total configurations}: $5 \times 2 \times 2 = 20$ unique configurations

\subsection{Dependent Variables (Performance Metrics)}

\textbf{Primary metric}:
\begin{itemize}
    \item \textbf{Total yield}: Cumulative fruit harvested by all agents over episode
\end{itemize}

\textbf{Secondary metrics}:
\begin{itemize}
    \item \textbf{Messages sent}: Total communication cost
    \item \textbf{Efficiency ratio}: $E = Y / M$ where $Y$ is yield and $M$ is messages (yield per message)
    \item \textbf{Coverage}: Number of unique cells visited (exploration measure)
    \item \textbf{Remaining fruit}: Fruit left unharvested at episode end
\end{itemize}

\subsection{Experimental Protocol}

\textbf{Replications}: 20 runs per configuration for reproducibility

\textbf{Random seeds}: Fixed sequence (0-19) for reproducibility

\textbf{Episode length}: 500 time steps (sufficient for agents to explore and harvest)

\textbf{Data collection}: Per-step metrics recorded via Mesa \texttt{DataCollector}

\textbf{Total runs}: $20 \text{ configurations} \times 20 \text{ replications} = 400$ simulation runs

\subsection{Baseline Comparison}

Range $r=0$ (no communication) serves as baseline. All other ranges are compared against this to quantify communication benefits.

\section{Results Analysis Approach}

The experimental results are analyzed using descriptive statistics including means, standard deviations, and confidence intervals. This approach is appropriate because:

\begin{itemize}
    \item The effect sizes are large and consistent across all configurations
    \item The practical significance is clear from the raw numbers
    \item The sample size (400 runs) provides sufficient evidence for robust conclusions
    \item Descriptive statistics effectively communicate the communication range effects
\end{itemize}

\subsection{Metrics Analyzed}

\begin{itemize}
    \item \textbf{Total yield}: Fruits harvested per run (primary metric for RQ1)
    \item \textbf{Total messages}: Number of messages sent (communication cost)
    \item \textbf{Efficiency}: Yield per message (cost-benefit ratio)
    \item \textbf{Coverage}: Number of unique cells visited (exploration metric)
    \item \textbf{Remaining fruit}: Unharvested fruit at episode end
\end{itemize}

\subsection{Comparison Method}

Results are compared across communication ranges using:

\begin{itemize}
    \item \textbf{Mean and standard deviation}: Central tendency and variability
    \item \textbf{Min/max values}: Range of outcomes
    \item \textbf{Percentage improvement}: Relative gain over baseline (range 0)
    \item \textbf{Efficiency analysis}: Yield per message to identify cost-benefit trade-offs
\end{itemize}

\subsection{Validation and Reproducibility}

\textbf{Fixed random seeds}: Ensures reproducibility of results

\textbf{Sanity checks}:
\begin{itemize}
    \item Range 0 should produce 0 messages
    \item Higher ranges should produce more messages
    \item Total fruit harvested cannot exceed initial fruit count (Static mode)
\end{itemize}

\section{Theoretical Discussion: Resource Dynamics (RQ2)}

While RQ1 focuses on communication range effects in Static environments, RQ2 asks whether resource dynamics (Static vs. Replenishing) change the optimal communication range. Due to time constraints, I don't run full experiments for RQ2, but discuss it theoretically here.

\subsection{Hypothesis}

\textbf{Static environments}: Communication benefits diminish over time as fruit depletes. Optimal range may be lower since there's less information to share as the episode progresses.

\textbf{Replenishing environments}: Sustained communication remains valuable because new fruit keeps appearing. Optimal range may be higher to maintain awareness of regenerating resources across the environment.

\subsection{Theoretical Framework}

The value of communication depends on information freshness and relevance. In Static mode, once an area is cleared, communicating about it provides no benefit. In Replenishing mode, previously cleared areas may have new fruit, making sustained communication more valuable.

This suggests an interaction effect: optimal communication range may depend on resource dynamics. Future work should test this hypothesis systematically.

\section{Summary}

This chapter presented the methodology for investigating RQ1: "Does communication range affect harvest yield?" I discussed four alternative approaches (RL, EA/PSO, game theory, parameter sweep) and justified choosing systematic parameter sweep with statistical analysis. The experimental design tests 5 communication ranges across 20 configurations with 20 replications each (400 total runs) in static environments. Statistical analysis uses descriptive statistics and hypothesis testing to determine the significance of communication range effects. RQ2 (resource dynamics) and machine learning extensions are discussed as future work.

The next chapter describes the implementation details of the simulation system.